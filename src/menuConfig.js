// 菜单配置
// headerMenuConfig：头部导航配置
// asideMenuConfig：侧边导航配置

const asideMenuConfig = [
  {
    name: 'Justitia Development Weekly',
    path: '/',
    icon: 'chart',
  }
];

const headerMenuConfig = asideMenuConfig;

export default headerMenuConfig;
